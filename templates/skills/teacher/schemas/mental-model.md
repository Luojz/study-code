# 项目心智模型
# 随学习展开迭代更新

## 架构分层

```
┌─────────────────────────────────────────────────┐
│  项目架构（根据实际项目更新）                     │
├─────────────────────────────────────────────────┤

┌───────────┐  ┌───────────┐  ┌───────────┐
│  页面层    │  │  业务逻辑层  │  │  基础设施层  │
│  HTML/Vue  │  │  JS 模块    │  │  工具/桥接  │
└───────────┘  └───────────┘  └───────────┘
     ↑ 展示             ↑ 处理数据         ↑ 被复用
     ↑ 事件绑定           ↑ 状态管理        ↑ 调原生能力
```

（根据 `/teach:init` 的扫描结果填充具体内容）

## 模块关系

（随展开迭代更新，记录已发现的模块间依赖关系）

## 数据流向

（随展开迭代更新，记录关键数据流向）

## 已学概念映射

| 概念 | 所属层 | 在架构图中的位置 |
|------|--------|----------------|
| （示例）SeaJS define | 基础设施 | 模块加载机制 |
| （示例）CiticsNative | 基础设施 | 原生桥接层 |
